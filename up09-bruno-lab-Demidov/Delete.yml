info:
  name: Delete
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/api/items/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
